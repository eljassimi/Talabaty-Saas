# Development profile: run backend (8080) + frontend separately (npm run dev, e.g. 3000 or 3030)
# Activate with: SPRING_PROFILES_ACTIVE=dev or -Dspring.profiles.active=dev
#
# After YouCan OAuth, redirect the browser to the frontend dev server.
# Use 3030 if you set Vite server.port to 3030 in talabaty-frontend/vite.config.ts
app.frontend.url=http://localhost:3000
